//multi line string using template string

let variableName = 10;

let p = `
This is a multi-line string
that spans across
multiple lines.
You can include variables like this: ${variableName}.
`;

console.log(p);
