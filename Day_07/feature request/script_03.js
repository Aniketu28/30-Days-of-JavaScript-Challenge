const books = {
    author:"Aniket",
    title:"do it",
    year:2024,
}

for(let book of Object.keys(books)){
    console.log(book);
}

for(let book of Object.values(books)){
    console.log(book)
}

